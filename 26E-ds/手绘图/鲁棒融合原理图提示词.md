## 服务段落

本提示词服务于论文《复杂场景下多模态情感预测的数学建模与算法设计》的"问题二模型建立与求解"章节，用于在给出鲁棒预测模型的公式之前，用一张原理图说明缺失感知融合机制的作用方式。该图说明输入的三模态时序表示与缺失掩码如何经可信度门控逐位置加权，再经跨模态注意力交互与位置聚合形成样本表示，最终输出情感极性与情感强度。图的依据来自本项目问题二的主模型结构，用到的知识包括缺失掩码表示、可信度门控、掩码归一化融合、跨模态自注意力与注意力池化五类机制；机制的关键关系是：掩码为硬约束把缺失位置的权重置零，门控在可用位置上重新分配模态权重，注意力在剩余可用位置之间传播上下文，池化把时序证据压缩为样本级判断。图中不出现任何具体数值、维度数字、激活函数符号与实验结果。

## 生成图片的提示词

生成一张用于学术论文的中文模型机制示意图，左中右三段式，纯白背景，无外框，无阴影，无渐变，无三维透视。

版面结构：画面左侧为输入端，自上而下三个等宽圆角矩形，分别标注"文本时序表示"、"语音时序表示"、"视觉时序表示"；每个矩形右侧紧邻一个窄条圆角矩形，标注"可用性掩码"，用深浅两段色块表示可用与缺失两种状态，窄条与主矩形之间用细虚线连接。

画面中部为机制区，自上而下三个层级。第一层为一个横跨三个模态的宽圆角矩形，标注"可信度门控"，三个模态各引一条箭头指向它，箭头旁边标注"逐位置加权"。第二层为一个宽圆角矩形，标注"掩码归一化融合"，由第一层向下引一条粗箭头连接。第三层为一个宽圆角矩形，标注"跨模态注意力交互"，由第二层向下引一条箭头连接，并在该层左侧画一条从输出回指的弧线箭头，标注"上下文传播"。

画面右侧为输出端，自上而下三个圆角矩形，分别标注"位置重要性权重"、"情感极性预测"、"情感强度预测"；由第三层向右引两条箭头分别指向后两个矩形，由第三层向上引一条箭头指向第一个矩形。

在画面底部画一条横贯的浅灰底圆角条，标注"缺失位置的权重恒为零：缺失信息不参与任何融合与聚合"。

配色：主色家族为深蓝（#0F4D92）与青蓝（#3775BA），文本通道用深蓝、语音通道用橙（#E28E2C）、视觉通道用青绿（#42949E），机制区统一用青蓝，缺失状态用浅灰（#D8D8D8），箭头与分隔线用中性灰（#8A8A8A）。节点为白底细边框，边框与所属分区主色一致，节点文字为黑色。字体为无衬线中文字体，分区标题加粗，其余文字字号一致。

布局原型：左中右三段式机制图；阅读方向：自左向右、自上而下；主色家族：深蓝—青蓝—橙—青绿；区分点：输入端用模态主色区分三路，机制区统一用青蓝并逐层加深，输出端用深蓝，底部约束条用浅灰底。

参考图组：本 skill 内置参考图组位于 assets/reference-pictures/，包含 Chen_AerialMind_2026.jpg、Han_VideoEspresso_2025.jpg、Liu_Controllable_2025.jpg、Liu_EEG2Video_2024.jpg、Liu_SLAM_2024.jpg、Ni_ColNeRF_2024.jpg 与三张版式截图；借鉴点：框架的分区与层级组织方式、结构的对称与对齐、排版的留白比例、思维链的自输入经机制到输出的推进顺序、配色的低饱和学术风格；仅借鉴形式，不复制具体内容、数据、标签、结论。

硬约束：图中所有可见文字必须为规范的简体中文，不得出现英文单词、拼音或乱码；不得出现数字、公式、变量符号、维度标注与激活函数名称；不得出现水印、署名、标志与装饰性图形；不得渲染流程管理类与项目治理类词汇为图内可见文字；同一层级节点等宽等高，箭头方向统一且端点不压住节点边框；节点文字居中且不溢出；四周留白均匀，不裁切任何文字与节点。

## 硬约束

- 图内可见文字全部为简体中文，分区标题与节点不得混入英文、拼音、乱码或方框替代字符。
- 不渲染任何数值、公式、变量符号、维度数字与函数名。
- 不渲染流程管理类与项目治理类词汇为图内可见文字。
- 不使用三维透视、阴影、渐变与照片素材；保持二维矢量风格的学术插图观感。
- 导出分辨率不低于 1200×800，长边与短边比例适合单栏正文插图，四周留白均匀，不裁切任何文字与节点。
