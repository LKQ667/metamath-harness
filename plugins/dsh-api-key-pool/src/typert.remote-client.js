import { API_KEY_POOL_INVOCATIONS } from './typert-shared.js';

export const TYPERT_REMOTE = Object.freeze({
  package: '@deepseek-harness/dsh-api-key-pool',
  descriptors: API_KEY_POOL_INVOCATIONS,
});

export default TYPERT_REMOTE;
