import { MATHMODEL_INVOCATIONS } from './typert-shared.js';

export const TYPERT = Object.freeze({
  package: '@deepseek-harness/dsh-mathmodel',
  face: 'host',
  schemas: [],
  invocations: MATHMODEL_INVOCATIONS,
  model: { services: [], events: [], objects: [] },
});
