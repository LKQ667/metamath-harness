# 三轮自查报告

本报告记录最终交付前的三轮全链路自查，覆盖内容与数据链、论文与版式链、代码与复现链。
三轮均通过后进入最终提交。

## 第一轮：内容与数据链

### 结论

第一轮检查内容与数据链，确认数据来源、模型推导与结果引用三者一致，通过。

### 数据链核对

- 全部数据来自赛题目录下的附件 1、附件 2 与附件 3，逐条登记在 `data/source_map.md`，
  未引入任何外部新增数据。
- 附件 1 的 241 条工况记录与附件 2 的 145 条半径记录经规整后写入派生数据文件，规整过程
  只做去重、缺失剔除与时间排序，未做平滑或截断。
- 附件 1 只覆盖 14400 s，恒温干燥阶段取末值作为恒定工况的处理已在论文假设 7 与各问
  result.md 中标注，属于建模假设而非数据补齐。

### 模型推导核对

- 五、模型建立与求解按“任务目标—变量与适用条件—基础关系—公式推导—离散与算法—图表结果—
  结果解释”的顺序推进，推导链完整，公式编号连续且唯一。
- 核心公式包括质量守恒方程、能量守恒方程、第三类边界条件、特征时间定义、有限体积半离散
  方程、烘干判据与移动边界变换，共 8 处编号，其余为无编号的定义式与中间式。
- 结论推进有条理：每个结果都回扣本问承诺，例如问题一的 1.34 cm 扰动深度回扣特征时间比，
  问题三的 57.1 h 回扣 2 至 3 天的题面量级，问题四的 50.8 h 回扣收缩加速干燥的机理判断。

### 自然文风核对

- 自然衔接：段落之间用因果、转折、条件与指代衔接，未使用“首先、其次、然后、接着”一类
  机械连接词。
- 句式长短变化：关键结论用短句收束，推导背景与限定条件用复合句展开，未出现整段句式同构。
- 括号密度：正文可见论述中的圆括号、方括号与中文方括号合计密度低于每千字 4 组。
- 第一人称必要性：仅在说明本文处理方式与假设时少量使用“本文”，未用于制造人工感。

### 数据与结果一致性

- `results/final_results.json` 是唯一结果源，论文正文、摘要与各问 result.md 引用的数值
  与该文件一致。
- 关键数值抽查：问题一表面温度 36.7863 ℃、中心温度 33.5765 ℃、表面含水率 1.5104；
  问题二表面温度 50.0048 ℃、中心温度 49.9065 ℃；问题三烘干时间 57.1 h；问题四烘干时间
  50.8 h、收缩比例 0.4000，全部可在结果源中逐项对应。

### 本轮新增门禁检查

- 正文符号：正文未出现项目符号与复选框符号，分层表达改用自然段、编号句与表格。
- 支撑材料目录：只写代码名称与重要 Excel 结果名称，未写路径、目录或“见某文件夹”。
- 摘要页：摘要与关键词全部位于第一页，摘要有效文字约 900 字，在不跨页的前提下尽量写满。
- 正文章节白名单：正文主章节严格为问题重述、问题分析、模型假设、符号说明、模型建立与
  求解、敏感度分析、模型评价与改进七节。
- 废话主章节清理：未出现候选方法比较、体系结构、边界讨论、问题衔接、方法论启示、
  整体总结、数据结果回扣、可复现性、运行顺序等独立主章节。
- 模型建立与求解深化证据：该节包含 8 处编号公式、有限体积离散推导、算法流程、两张
  验证表格与参数量纲核对表，深化证据充分。
- 普通柱状图必要性：全文未使用柱形图。排名与对比改用棒棒糖图，过程量改用折线与等值图，
  区间量改用线段与端点标记，符合柱状图策略为“少用”的要求。
- 顶刊一区中文三维图可行性评估：已评估。问题一的温度场与问题三的含水率场都具有时间与
  半径两个自变量，适合用三维曲面表达，本文各采用一张三维响应面；其余图属于剖面、等值线、
  棒棒糖与热图，用二维表达更清晰，因此不采用三维形式。
- 正文页数是否达标：达标，正文 26 页，不少于目标 25 页。

## 第二轮：论文与版式链

### 结论

第二轮检查论文与版式链，确认模板一致、编译无硬错误、图表全部入文，通过。

### 模板与结构

- 论文以技能内置模板为基座增量替换，保留 `\titlefont`、`\sectiontitlefont`、
  `\keywordfont`、`\appendixtitlefont` 等样式命令，未使用 `\maketitle`、`\tableofcontents`
  与普通 `abstract` 环境。
- 正文边界标记齐全：`body:start` 位于 `\begin{document}` 之后，标题、摘要、关键词与
  参考文献全部落在 `body:end` 之前，附录另起一页。

### 摘要页约束

- 摘要一页内：摘要与关键词全部位于第一页，未跨页。
- 摘要尽量写满：摘要有效文字约 900 字，落在 800-1000 字的建议区间内。
- 摘要结束标记：关键词之后保留 `abstract:end`，全文仅出现 1 次。
- 摘要编译页码复核：由 `main.aux` 复核，`abstract:end` 记录页码为 1，满足一页内要求。

### 正文边界

- 正文起始页：1
- 正文结束页：26
- 附录起始页：27
- 正文实际页数：26
- 参考文献计入正文，支撑材料目录与代码附录排除，附录另起页，正文结束页与附录起始页
  相差 1 页。

### 图表与版式

- 全文入文图片 16 张，与技术路线图、数据预处理、四问结果与灵敏度分析一一对应，无重复图
  与换色变体。
- 技术路线图由 Draw.io 链路生成并插入正文，源文件、PNG、SVG、PDF 齐全。
- 正文未使用强制分页、大段 `\vspace`、`\vfill` 或空图表占位；未出现连续大量空行。
- 正文未显示内部文件路径与绝对路径，仅保留原理图提示词的相对路径。

### 文风门禁

- 模板摘要示例与实际论文通过文风门禁：模板自带的摘要示例段落与本论文的实际摘要段落均通过
  文风门禁，机械连接词零命中，括号密度低于每千字 4 组，粗体只落在关键短语上。
- 编译日志无未定义控制序列、无缺字、无图片缺失、无交叉引用未更新。

## 第三轮：代码与复现链

### 结论

第三轮检查代码与复现链，确认源代码未被修改、附录净化边界正确、原版与净化版结果一致，通过。

### 代码分区

- `scripts/` 只保留共享工具、总控脚本与检查脚本，包含径向热湿耦合求解器、边界工况读取、
  绘图工具、结果聚合与数值验证脚本。
- 四个问题目录各自保留主脚本、README.md、result.md 与 figures/ 子目录，代码与结果链路
  可逐问追溯。

### 附录代码

- 真实源代码未修改：`数据预处理/prepare_data.py`、`scripts/ambient.py`、
  `scripts/drying_model.py`、`Q1/solve_q1.py` 至 `Q4/solve_q4.py` 的哈希在净化前后保持
  不变。
- 局部改名边界正确：本次未对局部变量改名，`rename_map` 为空，因此参数、公共接口、全局量、
  导入名、函数名、类名、属性名、字典键与字符串全部保持原样。
- 注释与空行已清理：净化副本由抽象语法树重新生成，不含注释、文档字符串、Markdown 围栏、
  装饰分隔线与连续空行。
- 代码结构已复核：净化副本未出现超长函数、过深嵌套、重复大段逻辑或无意义包装。
- 原版与净化版运行结果一致：在隔离临时项目中用同一命令分别运行原版与净化版，五组回归
  用例全部通过，结构化输出文件的哈希完全一致。

### 复现路径

- 复现命令、依赖环境、最终结果与异常处理记录写入 README.md。
- 数值验证脚本独立运行，输出网格收敛性、时间步无关性、质量守恒与移动边界退化检验的
  结构化结果。
- 时间步无关性：问题三在 20 s、10 s、5 s 三种步长下烘干时间分别为 57.1 h、57.1 h 与
  57.0972 h。
- 退化检验：把问题四求解器的半径设为常数 2 cm 并取附录 3 物性，烘干时间还原为 57.1 h，
  与问题三一致。

## 最终通过结论

三轮自查全部通过。第一轮确认内容与数据链一致，第二轮确认论文与版式链合规，第三轮确认
代码与复现链可追溯。项目可以进入最终交付。
